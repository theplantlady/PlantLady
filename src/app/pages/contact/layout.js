export const metadata = {
    title: "Plant Lady -- Contact",
    description: "Plant Lady is a next js and tailwind css website",
};

export default function ContactLayout({ children }) {
    return children;
}
